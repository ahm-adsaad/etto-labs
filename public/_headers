# Cloudflare Workers static assets headers (copied to ./out by next build).
# The generated apple-icon has no file extension, so set its type explicitly.
/apple-icon
  Content-Type: image/png

/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
